# 3seventeen Hub
# build/ = obfuscated scripts (committed, pushed, served via raw URL)
# dist/  = readable source (local only, never pushed)

# Ignore source
dist/
games/
loader.lua
premium_loader.lua
build.sh
keygen.sh
site/
.env
.env.example

# Track build output
!build/
!build/**

# OS files
.DS_Store
*.swp
*~
